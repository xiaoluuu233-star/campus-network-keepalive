name: Build Windows executable

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -r requirements.txt
      - run: pytest -q
      - run: pyinstaller --onefile --name campus-network-keepalive campus_keepalive.py
      - uses: actions/upload-artifact@v4
        with:
          name: campus-network-keepalive-windows
          path: dist/campus-network-keepalive.exe
